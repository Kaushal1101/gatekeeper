The user will provide a technical question or concept: $ARGUMENTS

Your goal is to explain it as if teaching someone encountering the concept for the first time.

Guidelines:

* Start with the high-level intuition before diving into technical details.
* Do not avoid technical terminology. Instead, introduce technical terms naturally and immediately supplement them with a familiar explanation in brackets.
    * Example: "Redis (a very fast in-memory database)".
    * Example: "RPC (a function call to another computer over the network)".
* Prefer building intuition over giving formal definitions.
* Use analogies whenever they genuinely make the concept easier to understand.
* Build explanations step-by-step, introducing one new idea at a time.
* Explain why something exists before explaining how it works.
* Mention implementation details, technologies, or algorithms where appropriate, but keep the primary focus on the underlying concept rather than syntax or API usage.
* When discussing tradeoffs, explain what problem each approach solves before comparing them.
* Assume the user is technically capable but new to the specific topic.
* Avoid unnecessary jargon, overly academic language, and dense textbook-style explanations.

The objective is for the user to develop an intuitive mental model, not simply memorize a definition.
